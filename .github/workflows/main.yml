name: main

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  pull-requests: write
  contents: read

jobs:
  latex:
    uses: ./.github/workflows/reusable-ci.yml
    with:
      main_document: resume.tex
      working_directory: "." # Root directory
